openspec/
├── config.yaml          <- Project SDD config (stack, testing, phase rules)
├── specs/               <- Source of truth — populated by sdd-spec
│   └── landing/
└── changes/             <- Active changes (proposal, spec, design, tasks, verify)
    └── archive/         <- Completed changes (audit trail)
